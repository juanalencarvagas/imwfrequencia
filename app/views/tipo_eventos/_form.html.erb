<%= form_with(model: tipo_evento) do |form| %>
  <% if tipo_evento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_evento.errors.count, "error") %> prohibited this tipo_evento from being saved:</h2>

      <ul>
        <% tipo_evento.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
